<nav class="navbar navbar-expand-lg navbar-inverse">
  <a class="navbar-brand ml-5" href="/">성감대다이어리</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="nav-item active"><%= link_to "게시판", posts_path, class: "nav-link"%></li>
        <li class="nav-item active"><%= link_to "다이어리", diaries_path, class: "nav-link"%></li>
        <li class="nav-item active"><%= link_to "STATIC", chart_path, class: "nav-link"%></li>
        <li class="nav-item active"><%= link_to "ABOUT", about_path, class: "nav-link"%></li>
            
      <% end %>
    </ul>

    <ul class="navbar-nav navbar-right navbar-nav mr-5">
      <% if user_signed_in? %>
        <div class="dropdown show">
          <a class="btn btn-secondary dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= current_user.name %> 님
          </a>

          <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
            <%= link_to "프로필 수정",  edit_user_registration_path, class: "dropdown-item" %>
            <%= link_to "로그아웃", destroy_user_session_path, method: :delete, data: { confirm: "로그아웃 하시겠습니까?" } , class: "dropdown-item"%>
          </div>
        </div>
      <% else %>
        <li class="nav-item active mr-3"><%= link_to "로그인", new_user_session_path, class: "nav-link btn btn-outline-dark"%></li>
        <li class="nav-item active"><%= link_to "가입", new_user_registration_path, class: "nav-link btn btn-dark"%></li>
      <% end %>
      
    </ul>
  </div>
 </nav>
